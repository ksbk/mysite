"""Pages app configuration."""

from django.apps import AppConfig


class PagesConfig(AppConfig):
    """Django app configuration for pages."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.pages"
